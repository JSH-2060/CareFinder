<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.carefinder.dao.height.HeightDAO">

    <!-- 키 기록 추가 -->
    <insert id="insert">
        INSERT INTO height_record
            (childId, height, recordDate)
        VALUES
            (#{childId}, #{height}, #{recordDate})
    </insert>

    <!-- 아이별 키 목록 조회 -->
    <select id="findByChildId" resultType="com.carefinder.dto.height.HeightDTO">
        SELECT
            heightId,
            childId,
            height,
            recordDate,
            created_at AS createdAt
        FROM height_record
        WHERE childId = #{childId}
        ORDER BY recordDate DESC
    </select>

    <!-- 키 기록 수정 -->
    <update id="update">
        UPDATE height_record
        SET
            height = #{height},
            recordDate = #{recordDate}
        WHERE heightId = #{heightId}
    </update>

    <!-- 키 기록 삭제 -->
    <delete id="delete">
        DELETE FROM height_record
        WHERE heightId = #{heightId}
    </delete>

</mapper>