<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.carefinder.dao.vaccine.VaccineDAO">

    <select id="selectByChild" resultType="com.carefinder.dto.vaccine.VaccineDTO">
        SELECT * FROM vaccine
        WHERE mno = #{mno}
        <choose>
            <when test="childId == 0 or childId == null">
                AND childId IS NULL
            </when>
            <otherwise>
                AND childId = #{childId}
            </otherwise>
        </choose>
        ORDER BY inoculationDate ASC
    </select>

    <select id="selectOne" resultType="com.carefinder.dto.vaccine.VaccineDTO">
        SELECT * FROM vaccine
        WHERE vaccineNo = #{vaccineNo}
    </select>

    <insert id="insert">
        INSERT INTO vaccine (
            mno,
            childId,
            vaccineName,
            chasu,
            inoculationDate,
            status
        ) VALUES (
                     #{mno},
                     #{childId, jdbcType=INTEGER},
                     #{vaccineName},
                     #{chasu},
                     #{inoculationDate},
                     'N'
                 )
    </insert>

    <update id="updateStatus">
        UPDATE vaccine
        SET status = #{status}
        WHERE vaccineNo = #{vaccineNo}
    </update>

    <delete id="delete">
        DELETE FROM vaccine WHERE vaccineNo = #{vaccineNo}
    </delete>

    <update id="update">
        UPDATE vaccine
        SET vaccineName     = #{vaccineName},
            chasu           = #{chasu},
            inoculationDate = #{inoculationDate}
        WHERE vaccineNo = #{vaccineNo}
    </update>

    <select id="selectChildList" resultType="com.carefinder.dto.child.ChildDTO">
        SELECT * FROM child
        WHERE mno = #{mno}
        ORDER BY childId ASC
    </select>

</mapper>