<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

    <!-- 카카오 ID로 회원 조회 -->
    <select id="findByKakaoId" parameterType="string"
            resultType="com.carefinder.dto.member.MemberKakaoDTO">
        SELECT *
        FROM member
        WHERE kakao_id = #{kakaoId}
    </select>

    <!-- 카카오 회원 신규 가입 -->
    <insert id="insertKakaoMember" parameterType="com.carefinder.dto.member.MemberKakaoDTO">
        INSERT INTO member (id, pw, kakao_id, email, name, gender, phoneNumber, birth,age)
        VALUES (#{id}, #{pw}, #{kakaoId}, #{email}, #{name}, #{gender}, #{phonenumber}, #{birth},#{age})
    </insert>

</mapper>

